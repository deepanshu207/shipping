# Legacy path — do not use for development

Site files moved to **repo root** (`../index.html`, `../own-api.js`, etc.).

- Local dev: run `python server.py` from repo root
- Deploy: `wrangler.jsonc` uses repo root, not this folder

This folder only exists so old `/supplierhub.in/` bookmarks redirect to `/`.
